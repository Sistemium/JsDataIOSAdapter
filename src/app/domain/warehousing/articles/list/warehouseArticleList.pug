.warehouse-article-list

  .list-group.scroll-y(
  resize resize-offset-top="5"
  vs-repeat="74"
  )
    a.list-group-item(
    href
    ng-repeat='item in vm.articlesFiltered track by item.id'
    ng-click="vm.articleClick(item)"
    )
      .title
        warehouse-article(ng-model="item")
        //.name {{ ::item.name }}
      .footer
        .producer {{ item.producer.name }}
