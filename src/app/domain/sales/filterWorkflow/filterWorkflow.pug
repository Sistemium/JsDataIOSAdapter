.filter-workflow

  a(
  ng-if='vm.workflowDictionary'
  href
  uib-popover-template="'app/domain/sales/filterWorkflow/filterWorkflowPopover.html'"
  popover-placement='bottom-left auto'
  popover-is-open='vm.popoverIsOpen'
  )
    span.title(ng-if='vm.currentWorkflow') Статус:
    span.name {{ vm.workflowDictionary[vm.currentWorkflow].label || 'Фильтр по статусу' }}
    span.caret

