'use strict';

(function () {

  function PhotoReportsController(/*Schema*/) {

    // const {PhotoReport} = Schema.models();

  }

  angular.module('webPage')
    .controller('PhotoReportsController', PhotoReportsController);

}());
