.campaign-popover-template

  uib-tabset.tabset(active='vm.teamIdx')
    uib-tab(
    index='$index + 1'
    ng-repeat='team in vm.teams'
    heading='{{team.name}}'
    select='vm.onTeamSelect(team, $index)'
    )

  #campaign-popover-scroll.scroll-y(
  elem-ready='vm.onElemLoad()'
  resize
  resize-offset-top='30'
  )

    .wrapper
      #scrollable-div(
      vs-repeat='110'
      vs-scroll-parent='#campaign-popover-scroll'
      )

        .campaign(ng-repeat='campaign in vm.selectedTeam.campaigns track by campaign.id')

          .name {{ campaign.title }}

          .pictures
            a.thumb(
            href
            ng-click='vm.thumbClick(picture)'
            ng-repeat='picture in ::campaign.campaignPictures | orderBy: "name" | limitTo: 4 track by picture.id'
            )
              img(ng-src='{{picture.srcThumbnail}}')
