.copy-selected

  button.btn.btn-danger(
  ng-if='vm.inProgress'
  ng-click='vm.cancelClick($event)'
  ) Отмена

  button.btn(
  ng-click='vm.triggerClick()'
  ng-disabled='vm.inProgress && !vm.isReady()'
  ng-class='vm.isReady() && "btn-success" || vm.inProgress && "btn-warning" || "btn-primary"'
  ng-if='vm.isIos()'
  )
    i.glyphicon.glyphicon-copy(ng-if='!vm.inProgress')
    span(ng-if='vm.inProgress') Готово, скопировать
